.. include:: common.defs

.. _completed-ats-projects:

Completed Projects
***********************

For historical purposes projects which were planned here but are now completed (committed to master)
are listed in this section.

BufferWriter
   Implemented for 8.0.0.

C++17 :code:`string_view`
   Implement as `ts::string_view` and `ts::TextView`.

Event Loop Changes
   Change the event loop to be able to wait on I/O instead of condition variables.

Forwarded Header
   Implemented for 8.0.

Thread Initialization
   Do thread initialization through continuations executed at the start of a thread, instead of post facto cleanup.

TLS Bridge
   Committed for 8.0.0.

MemArena
   Committed for 9.0.0.

Certifier
   Commited for 9.0.0.
